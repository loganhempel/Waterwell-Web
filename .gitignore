node_modules/
.DS_Store
*.log
package-lock.json
package.json

# Internal / sensitive — never push to this PUBLIC repo
Kohei-voicenote-*-TRANSCRIPT.txt
*-TRANSCRIPT.txt
SOURCE-OF-TRUTH.md
enquiry-data-2026-07-01/
HANDOFF.md
Waterwell_DigitalConsult_Strategy.md
Waterwell_DigitalConsult_Strategy.pdf
relaunch-2026-06/
.claude/
Waterwell-June-Report*
tools/

# Impeccable critique snapshots — internal design notes, not for the public repo
.impeccable/

# Archived site versions — contain Google Ads keyword/negative/RSA exports
# and a client plan xlsx. This repo is PUBLIC.
_versions/

# Client-facing deliverables — sent directly, never served from the site
Waterwell-Update-for-Kohei.*
Waterwell-Build-Summary-for-Kohei.*

# Outstanding legal questions for the client — internal, never publish
LEGAL-NEEDS-KOHEI.md
Website update notes*.docx

# Vercel CLI link data — this repo is PUBLIC, keep project/org ids out of it
.vercel/

# Full-size photo originals from the client (19 Aug drop) plus licensed stock
# masters pulled from his Drive. The site serves the processed crops in
# assets/; the originals are large and, in the stock case, licensed to Kohei —
# neither belongs in a PUBLIC repo.
assets/incoming/

# Drafted client emails — internal, sent from Gmail, never served
replies/
